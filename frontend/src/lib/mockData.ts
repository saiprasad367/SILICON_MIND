// mockData.ts — REMOVED
// All data is now served from the Flask backend AI engine.
// No mock data is used anywhere in this application.

export const mockAnalysis = undefined;
export type Analysis = never;
